import { AISoother } from './AISoother';
import type { ComputedStats } from '../lib/types';
/** Kept as a compatibility wrapper for earlier stage wiring. */
export function AskHelper(_props: { stats: ComputedStats }) { return <AISoother />; }
